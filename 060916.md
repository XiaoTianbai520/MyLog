# 260916

### 1.分库分表情况分析

1. IO瓶颈：热点数据太多，数据库缓存不足，产生大量磁盘IO，效率较低。 请求数据太多，带宽 不够，网络IO瓶颈。 
2. CPU瓶颈：排序、分组、连接查询、聚合统计等SQL会耗费大量的CPU资源，请求数太多，CPU出 现瓶颈

### 2.MyCat结构

![image-20260916104952692](C:\Users\c1338\AppData\Roaming\Typora\typora-user-images\image-20260916104952692.png)

每个逻辑库中分为多个逻辑表，每个逻辑表通过分片规则选分片节点，每个分片节点接一个节点主机。

### 3.分库分表通用逻辑

逻辑库**schema**：必要属性name，有哪些逻辑表

通过tabe的name进行关联

逻辑表**table**：必要属性name，有哪些dataNode，分片规则rule，指定分片规则

通过dataNode的name进行关联

**dataNode**：必要属性name，节点对应主机dataHost，database指定主机的哪个数据库

通过datahost的name进行关联

**datahost**：配置连接信息，

writeHost的name可以随便写